<!-- Responsive panel example-->

<div class="row">
  <div class="col-sm-offset-1 col-sm-10 col-md-offset-2 col-md-8">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Source #<%= @source.id %></h3>
      </div>
      <div class="panel-body">
        <div class="row">
          <div class="col-md-12">
            <dl class="dl-horizontal">
              <dt>Name</dt>
              <dd><%= @source.name %></dd>

              <dt>Address</dt>
              <dd><%= @source.address %></dd>

              <dt>City</dt>
              <dd><%= @source.city %></dd>

              <dt>State</dt>
              <dd><%= @source.state %></dd>

              <dt>Zip</dt>
              <dd><%= @source.zip %></dd>

              <dt>Website url</dt>
              <dd><%= @source.website_url %></dd>

            </dl>
            <div class="btn-group btn-group-justified">
              <%= link_to sources_url, :class => "btn btn-primary" do %>
                <i class="fa fa-chevron-left"></i>
              <% end %>
              <%= link_to edit_source_url(@source.id), :class => "btn btn-warning" do %>
                <i class="fa fa-edit"></i>
              <% end %>
              <%= link_to source_url(@source.id), :method => "delete", :data => { :confirm => "Do you really want to delete this source?" }, :class => "btn btn-danger" do %>
                <i class="fa fa-trash-o"></i>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- End responsive panel example -->
